# Copy to .env and set your Discord application (client) id.
# Public value (baked into the client bundle); the client SECRET lives only in
# the backend (backend-cred.env), never here.
VITE_DISCORD_CLIENT_ID=
